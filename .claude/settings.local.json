{
  "permissions": {
    "allow": [
      "Bash(which pyenv && pyenv --version 2>/dev/null || echo \"NOT_INSTALLED\")",
      "Bash(which uv && uv --version 2>/dev/null || echo \"NOT_INSTALLED\")",
      "Bash(git --version 2>/dev/null || echo \"NOT_INSTALLED\")",
      "Bash(which code && code --version 2>/dev/null || echo \"NOT_INSTALLED\")",
      "Bash(gh auth status 2>&1)"
    ]
  }
}
